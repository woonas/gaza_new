<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.gaza.myapp.myhome.MyHomeDAOInterface">
	<select id="memberRecord" resultType="kr.gaza.myapp.myhome.MyHomeVO">
		select membernamekor, membernameeng, flightcnt from member where membernum=#{membernum}
	</select>
	<select id="memberMiles" resultType="kr.gaza.myapp.myhome.MyHomeVO">
		select sum(a.point) from miles a join mymiles b on a.milesnum = b.milesnum where b.membernum=#{membernum}
	</select>
</mapper>