<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.gaza.myapp.eventPackage.alliance.EPallianceInterface">
<select id="AllianceVO" resultType="kr.gaza.myapp.eventPackage.alliance.AllianceVO">
	select  from event order by eventnum desc
</select>

<select id="AllianceVO" resultType="kr.gaza.myapp.eventPackage.alliance.AllianceVO">
	select eventnum, eventname, startdate, enddate, eventimg from event where eventnum = #{eventnum}
</select>
</mapper>