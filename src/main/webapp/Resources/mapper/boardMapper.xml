<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.gaza.myapp.board.noticeBoard.NoticeBoardInterface">
	<select id="noticeBoardTotalRecord" resultType="int">
		select count(noticenum) from noticeboard
	</select>
	<select id="getAllRecord" resultType="kr.gaza.myapp.board.noticeBoard.NoticeBoardVO">
		<![CDATA[
			select * from (select * from (select noticeNum, noticeType, subject, content, hit, to_char(regdate, 'YYYY.MM.DD') regdate
			from noticeBoard order by noticenum desc) where rownum<=#{param1} order by noticeNum asc) 
			where rownum<=#{param2} order by noticeNum desc
		]]>
	</select>
</mapper>